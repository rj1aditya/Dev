#include<bits/stdc++.h>
using namespace  std;


int fun(int val)
{
    vector<int, vector<int>> map;

    int counter;

    for(int i = 2; i <= val+1; i++)
    {
        counter = 1;
        while(i*counter <= val+1)
        {
            int x = i*counter;
            
        }
    }
}